"use client";
import React from 'react';
import { SectionWrapper } from '@/components/SectionWrapper';

const BonusesSection = () => (
  <SectionWrapper className="bg-yellow-100 text-center">
    <h2 className="text-3xl font-bold">Bonuses Section Placeholder</h2>
  </SectionWrapper>
);
export default BonusesSection;