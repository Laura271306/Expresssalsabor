"use client";
import React from 'react';
import { SectionWrapper } from '@/components/SectionWrapper';

const BenefitsSection = () => (
  <SectionWrapper className="bg-gray-100 text-center">
    <h2 className="text-3xl font-bold">Benefits Section Placeholder</h2>
  </SectionWrapper>
);
export default BenefitsSection;