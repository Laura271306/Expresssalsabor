"use client";
import React from 'react';
import { SectionWrapper } from '@/components/SectionWrapper';

const HeroSection = () => (
  <SectionWrapper className="bg-blue-100 text-center">
    <h1 className="text-4xl font-bold">Hero Section Placeholder</h1>
    <p className="mt-4">Conteúdo principal da página.</p>
  </SectionWrapper>
);
export default HeroSection;